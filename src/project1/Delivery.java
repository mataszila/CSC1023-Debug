package project1;

/**
 * HINT: DO NOT ALTER THIS CLASS!!!
 * 
 * This class defines some (public) fixed objects that represent the ticket
 * delivery method.
 * 
 * You can write expressions like: if (getMethod() == Delivery.POST) ...
 * 
 * The above expression is comparing the object returned by a getMethod() method
 * call with the fixed object that represents a deliver by post, and if they are
 * the same object
 */
public enum Delivery {
	POST, COLLECTION
}
