package project1;

/**
 * This class defines some (public) fixed objects that represent a ticket.
 * 
 * DO NOT ALTER THIS CLASS
 * 
 * You can write expressions like: if (t == Ticket.ADULT) ...
 * 
 * The above expression is comparing the object t with the fixed object that
 * represents an adult ticket, and if they are the same object ...
 */
public enum Ticket {
	ADULT, CHILD, SENIOR, STUDENT
}
